WinShutdown By J~Net 2026

https://github.com/jamieduk/winshutdown

This requires both systems to be on the same network and
you must use correct login credentails


sudo apt -y update
sudo apt install -y samba-common-bin

sudo chmod +x winshutdown.sh

./winshutdown.sh



or use a bash rc function (read that text file about that...

bash_rc_function_example.txt



~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
You may need to run these ps commands on windows target 1st (in admin cmd)

netsh advfirewall firewall set rule group="File and Printer Sharing" new enable=Yes
netsh advfirewall firewall set rule group="Remote Service Management" new enable=Yes
netsh advfirewall firewall set rule group="Windows Management Instrumentation (WMI)" new enable=Yes







